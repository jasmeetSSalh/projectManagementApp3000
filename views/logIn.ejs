<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login Page</title>
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Roboto:wght@400&display=swap"
      rel="stylesheet"
    />
    <style>
      body {
        font-family: "Roboto", sans-serif;
      }

      input {
        background-color: rgba(255, 255, 255, 0.2);
      }
    </style>
  </head>
  <body
    class="min-h-screen flex items-center justify-center bg-gradient-to-b from-gray-900 to-white"
  >
    <div class="p-8 w-96">
      <h1 class="text-5xl font-bold mb-16 text-center text-white">Login</h1>
      <form action="/users/login" method="POST" class="space-y-4">
        <div>
          <label
            for="username"
            class="flex items-center justify-center text-md font-medium text-gray-600"
            >Email:</label
          >
          <input
            type="text"
            id="email"
            name="email"
            required
            class="mt-1 p-2 w-full border rounded-md"
          />
        </div>
        <div>
          <label
            for="password"
            class="flex items-center justify-center text-md font-medium text-gray-600"
            >Password:</label
          >
          <input
            type="password"
            id="password"
            name="password"
            required
            class="mt-1 p-2 w-full border rounded-md"
          />
        </div>
        <div class="flex justify-between">
          <input
            type="submit"
            value="Login"
            class="bg-gray-900 text-white px-4 py-2 rounded-md hover:bg-black"
          />
          <!-- <input
            type="button"
            value="Sign Up"
            class="bg-gray-300 text-gray-900 px-4 py-2 rounded-md hover:bg-gray-400"
          /> -->
          <a href="signup" class="text-gray-300 hover:text-gray-400"
            >Don't have an account? Signup!</a
          >
        </div>
      </form>
    </div>
  </body>
</html>
